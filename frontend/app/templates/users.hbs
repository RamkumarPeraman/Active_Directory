<div class="user-grid">
  <h1 class="heading">Users</h1>
  <div class="tiles">
    {{#each this.users as |user|}}
      <div class="tile">
        <a href="#" {{on "click" (fn this.showUserDetails user.id)}}><img src="../asset/user.png" alt="Home" class="user_icon u_icon" /><br> {{user.first_name}}</a>
        <p></p>
      </div>
    {{else}}
      <p>No users found.</p>
    {{/each}}
  </div>
  {{#if this.selectedUser}}
    <div class="popup">
      <div class="popup-content">
        <button class="close-button" type="button" {{on "click" this.closePopup}}>X</button>
        <p>
            <strong>First Name:</strong>
            {{#if this.selectedUser.first_name}}
               <i>{{this.selectedUser.first_name}}</i>
            {{else}}
               <i>No First Name found</i>
            {{/if}}
        </p>
         <p>
            <strong>Last Name:</strong>
            {{#if this.selectedUser.last_name}}
               <i>{{this.selectedUser.last_name}}</i>
            {{else}}
              <i>No Last Name found</i>
            {{/if}}
        </p>
         <p>
            <strong>Phone Number:</strong>
            {{#if this.selectedUser.phone_number}}
               <i>{{this.selectedUser.phone_number}}</i> 
            {{else}}
               <i>No Phone Number found</i>
            {{/if}}
          </p>
      </div>
    </div>
  {{/if}}
</div>
